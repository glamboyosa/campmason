import React from 'react';
import Layout from '../Layout/Layout';
import RegisterContainer from '../../Containers/RegisterContainer/register';
const Register = () => {
  return (
    <Layout>
      <RegisterContainer />
    </Layout>
  );
};

export default Register;
